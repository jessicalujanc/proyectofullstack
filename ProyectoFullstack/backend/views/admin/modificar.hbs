<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Administrador</title>
  <link href="/backend/public/stylesheets/layout.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
    crossorigin="anonymous" />
</head>

<body>
  <div>
    <form action="/admin/comunidad/modificar" method="post" enctype="multipart/form-data">
      {{!-- porque estoy trabajando con imágenes --}}
      <div class="mb-3">
        <input type="hidden" name="id" value="{{comentario.id}}">
        <label for="nombre" class="form-label">Nombre</label>
        <input
          type="text"
          class="form-control"
          id="nombre"
          name="nombre"
          placeholder="Nombre"
          required
          value="{{comentario.nombre}}"
        />
      </div>
      <div class="mb-3">
        <label for="email" class="form-label">Email</label>
        <input
          type="email"
          class="form-control"
          id="email"
          name="email"
          placeholder="name@example.com"
          required
          value="{{comentario.email}}"
        />
      </div>
      <div class="mb-3">
        <label for="comentario" class="form-label">Comentario</label>
        <textarea
          class="form-control"
          id="comentario"
          name="comentario"
          rows="3"
          placeholder="Escriba su comentario aquí"
          required
        >{{comentario.comentario}}</textarea>
      </div>
      <div class="mb-3">
        <p>
          <label for="imagen" class="form-label">Agregar imagen</label>
          <input
            type="file"
            class="form-control"
            id="imagen"
            name="imagen"
          />
        </p>
        <p>
          <label for="img_delete">
            <input type="checkbox" name="img_delete" id="img_delete" value="1"> ¿Desea eliminar la imagen actual?
          </label>
          <input type="hidden" name="img_original" id="img_original" value="{{comentario.img_id}}">
        </p>
      </div>
      <button type="submit" class="btn btn-light">Modificar</button>
      {{#if error}}
      <h5>{{message}}</h5>
      {{/if}}
    </form>
  </div>
</body>

</html>